import { ConteudoDirective } from './conteudo.directive';

describe('ConteudoDirective', () => {
  it('should create an instance', () => {
    const directive = new ConteudoDirective();
    expect(directive).toBeTruthy();
  });
});
